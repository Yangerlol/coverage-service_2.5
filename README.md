# 📈 Проект: Анализ покрытия кода (Coverage Service)

## 📝 Описание проекта
Данный проект направлен на изучение инструментов оценки качества тестирования. В качестве примера используется сервис статистики, для которого настроен плагин **JaCoCo** для измерения процента покрытия кода тестами.

---

## 🛠 Технологический стек
* **Язык программирования:** Java 11
* **Система сборки:** Maven
* **Инструмент покрытия:** JaCoCo (Java Code Coverage)
* **Тестовая среда:** JUnit 5

---

## ⚙️ Особенности проекта
* **JaCoCo Plugin:** настроен в `pom.xml` для автоматической генерации отчетов.
* **Метрики:** анализ покрытия по строкам (lines), веткам (branches) и инструкциям.
* **Порог покрытия:** установка минимально допустимого процента прохождения тестов для успешной сборки.

---

## 🚀 Как запустить тесты и отчет
1. Склонируйте репозиторий:
   `git clone https://github.com/Yangerlol/coverage-service_2.5.git`
2. В терминале выполните команду для запуска тестов и генерации отчета:
   `mvn clean verify`
3. Откройте отчет в браузере:
   `target/site/jacoco/index.html`

---

## 📊 Пример настройки (pom.xml)

```xml
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.7</version>
    <executions>
        <execution>
            <goals>
                <goal>prepare-agent</goal>
                <goal>report</goal>
            </goals>
        </execution>
    </executions>
</plugin>
